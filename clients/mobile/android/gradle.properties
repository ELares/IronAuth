# AppAuth and its transitive dependencies are AndroidX; without this the build fails at
# checkDebugAarMetadata with a message that names the property but not the reason.
android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g
