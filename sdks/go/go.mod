// The generated management client is its own module so a consumer can `go get` it without
// the workspace, and so `go build` here proves the generator emits compiling Go.
module github.com/ELares/ironauth/sdks/go

go 1.22
