{
  "$comment": "Generated by scripts/flow-schema.sh; do not edit by hand.",
  "$defs": {
    "Autocomplete": {
      "description": "An `autocomplete` hint a browser uses to offer the right saved value (issue #84).",
      "oneOf": [
        {
          "const": "username",
          "description": "The account identifier.",
          "type": "string"
        },
        {
          "const": "current-password",
          "description": "The current password.",
          "type": "string"
        },
        {
          "const": "new-password",
          "description": "A new password (registration, password change).",
          "type": "string"
        },
        {
          "const": "one-time-code",
          "description": "A one time code (OTP, TOTP).",
          "type": "string"
        },
        {
          "const": "webauthn",
          "description": "A WebAuthn credential.",
          "type": "string"
        }
      ]
    },
    "FieldConstraints": {
      "description": "The effective validation constraints a configured signup field carries on the wire\n(issue #87): the trait sub-schema's closed keyword vocabulary, TIGHTENED by the form's\nnarrowing rule (the trait always applies, the form only ever narrows). This is a CLIENT\nHINT only: it is exactly what the server enforces authoritatively on submit, mirrored to\nthe wire so a hosted page or a reference SPA validates from ONE source. It carries no\nsecret and no value except the enumerated permitted values, which are the schema's own\nconfigured members, never user data. Every keyword is skip-if-none, so a field with no\nconstraint of a given kind omits it.",
      "properties": {
        "enum": {
          "description": "The permitted values, when the field is enumerated (the schema's configured members).",
          "items": true,
          "type": [
            "array",
            "null"
          ]
        },
        "maxItems": {
          "description": "The maximum array length.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "maxLength": {
          "description": "The maximum string length.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "maximum": {
          "description": "The inclusive numeric upper bound.",
          "type": [
            "number",
            "null"
          ]
        },
        "minItems": {
          "description": "The minimum array length.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "minLength": {
          "description": "The minimum string length.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "minimum": {
          "description": "The inclusive numeric lower bound.",
          "type": [
            "number",
            "null"
          ]
        },
        "type": {
          "description": "The effective primitive type name (`string` / `number` / `integer` / `boolean`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "FlowStateTag": {
      "description": "The current state machine position of a flow (issue #84). A single flat tag across all\njourneys, so a client renders any state from the same field.",
      "oneOf": [
        {
          "const": "identifier_password",
          "description": "The login identifier plus password first factor (also the login start state).",
          "type": "string"
        },
        {
          "const": "registration_details",
          "description": "The registration details (identifier plus password) first factor, the registration\nstart state.",
          "type": "string"
        },
        {
          "const": "registration_ack",
          "description": "The uniform registration acknowledgment (the #64 closed mode ack or the waitlist\npending notice): a terminal render that discloses nothing, so the flow stays OPEN.",
          "type": "string"
        },
        {
          "const": "mfa_challenge",
          "description": "A second factor challenge is required.",
          "type": "string"
        },
        {
          "const": "mfa_enroll",
          "description": "A second factor enrollment is required.",
          "type": "string"
        },
        {
          "const": "progressive_profiling",
          "description": "A progressive profiling step (issue #87): a subsequent login collects the client form's\nlater-login signup fields as a HELD login step, injected where a successful primary (plus\nany second factor) login would otherwise mint the session. The step is always SKIPPABLE\n(a missing required later-login field prompts but never blocks the mint), so the flow\nstays OPEN on a re-render and mints on a skip or a valid submission.",
          "type": "string"
        },
        {
          "const": "recovery_start",
          "description": "The recovery identifier entry (the recovery start state).",
          "type": "string"
        },
        {
          "const": "recovery_ack",
          "description": "The uniform recovery acknowledgment plus one-time-code entry: the #64 anti-enumeration\nrender (identical for a known and an unknown identifier) that ALSO carries the code\ninput, so an existing and an unknown identifier are indistinguishable while a genuine\nrecovery can still complete. The flow stays OPEN until a correct code mints the session.",
          "type": "string"
        },
        {
          "const": "federation_start",
          "description": "The federated login launcher: the federation node group whose submission produces the\nredirect to the EXISTING outbound federation authorize leg.",
          "type": "string"
        },
        {
          "const": "completed",
          "description": "The flow completed and a session was minted.",
          "type": "string"
        }
      ]
    },
    "InputType": {
      "description": "A typed form input type (issue #84).",
      "oneOf": [
        {
          "const": "text",
          "description": "A free text field.",
          "type": "string"
        },
        {
          "const": "password",
          "description": "A password field (never prefilled with a secret).",
          "type": "string"
        },
        {
          "const": "email",
          "description": "An email address field.",
          "type": "string"
        },
        {
          "const": "tel",
          "description": "A telephone number field.",
          "type": "string"
        },
        {
          "const": "hidden",
          "description": "A hidden field (a browser transport `return_to`, for example).",
          "type": "string"
        },
        {
          "const": "checkbox",
          "description": "A checkbox (remember this device, for example).",
          "type": "string"
        },
        {
          "const": "submit",
          "description": "A submit control.",
          "type": "string"
        }
      ]
    },
    "Journey": {
      "description": "The journey a flow drives (issue #84). One object renders every journey; there is no\njourney specific client logic.",
      "oneOf": [
        {
          "const": "login",
          "description": "Sign in with an existing account.",
          "type": "string"
        },
        {
          "const": "registration",
          "description": "Create a new account.",
          "type": "string"
        },
        {
          "const": "mfa",
          "description": "A second factor challenge or enrollment.",
          "type": "string"
        },
        {
          "const": "recovery",
          "description": "Account recovery.",
          "type": "string"
        },
        {
          "const": "federation",
          "description": "A federated (OIDC upstream) login launcher.",
          "type": "string"
        }
      ]
    },
    "Message": {
      "description": "One human readable message: a stable numeric id, its kind, the default locale render,\nand the structured context (issue #84). The id and the context are the localization\nkey; the text is a convenience.",
      "properties": {
        "context": {
          "$ref": "#/$defs/MessageContext",
          "description": "The structured parameters. Empty for a parameterless message."
        },
        "id": {
          "$ref": "#/$defs/MessageId",
          "description": "The stable numeric id (the localization key)."
        },
        "kind": {
          "$ref": "#/$defs/MessageKind",
          "description": "The message kind."
        },
        "text": {
          "description": "The default locale (`en`) render. A convenience only; i18n keys on `id`+`context`.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "kind",
        "text",
        "context"
      ],
      "type": "object"
    },
    "MessageContext": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "The structured parameters a localized render interpolates (issue #84): a stable, sorted\nkey/value map (`BTreeMap`, so an identical context serializes identically). The values\nare NEVER interpolated into the numeric id; the id selects the template and the context\nfills it. Empty for a message with no parameters.",
      "type": "object"
    },
    "MessageId": {
      "description": "A stable numeric message id (issue #84). Serialized as a bare integer, so a client\nkeys its localized copy on the number, never on the default text.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "MessageKind": {
      "description": "The kind of a message, so a client can style an error distinctly from an informational\nhint or a success note without parsing the copy.",
      "oneOf": [
        {
          "const": "info",
          "description": "An informational prompt, label, or title.",
          "type": "string"
        },
        {
          "const": "success",
          "description": "A success note (a step completed).",
          "type": "string"
        },
        {
          "const": "error",
          "description": "An error (a validation failure, an expiry, a uniform authentication failure).",
          "type": "string"
        }
      ]
    },
    "Node": {
      "description": "One UI node (issue #84): a group, its typed attributes, an optional label, and any\nmessages (validation errors or hints) attached to THIS node. The `sequence` fixes the\nnode's place within its group in the deterministic order and is not serialized.",
      "properties": {
        "attributes": {
          "$ref": "#/$defs/NodeAttributes",
          "description": "The typed attributes."
        },
        "group": {
          "$ref": "#/$defs/NodeGroup",
          "description": "The node group."
        },
        "label": {
          "anyOf": [
            {
              "$ref": "#/$defs/Message"
            },
            {
              "type": "null"
            }
          ],
          "description": "The node label (a message), or [`None`] for a label free node (hidden, text)."
        },
        "messages": {
          "description": "Errors and hints attached to THIS node (a per node validation error carries its\nmessage id here).",
          "items": {
            "$ref": "#/$defs/Message"
          },
          "type": "array"
        }
      },
      "required": [
        "group",
        "attributes",
        "messages"
      ],
      "type": "object"
    },
    "NodeAttributes": {
      "description": "The typed attributes of a node (issue #84): a tagged union over the renderable node\nkinds. Tagged by `node_type` so a client dispatches on one field.",
      "oneOf": [
        {
          "description": "A form input the client renders and submits.",
          "properties": {
            "autocomplete": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Autocomplete"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The `autocomplete` hint, or [`None`]."
            },
            "constraints": {
              "anyOf": [
                {
                  "$ref": "#/$defs/FieldConstraints"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The effective validation constraints (issue #87), for a configured signup field\nonly. [`None`] for every built in node (the identifier, the password, an OTP\ncode), so those nodes serialize byte identically to before the field was added."
            },
            "disabled": {
              "description": "Whether the control is disabled.",
              "type": "boolean"
            },
            "input_type": {
              "$ref": "#/$defs/InputType",
              "description": "The input type."
            },
            "name": {
              "description": "The form field name (for example `identifier`).",
              "type": "string"
            },
            "node_type": {
              "const": "input",
              "type": "string"
            },
            "required": {
              "description": "Whether the field is required.",
              "type": "boolean"
            },
            "value": {
              "description": "A prefill value (never a secret), or [`None`].",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "node_type",
            "name",
            "input_type",
            "required",
            "disabled"
          ],
          "type": "object"
        },
        {
          "description": "A block of rendered copy (the copy IS a message, so it localizes).",
          "properties": {
            "message": {
              "$ref": "#/$defs/Message",
              "description": "The message this text renders."
            },
            "node_type": {
              "const": "text",
              "type": "string"
            }
          },
          "required": [
            "node_type",
            "message"
          ],
          "type": "object"
        }
      ]
    },
    "NodeGroup": {
      "description": "A node group (issue #84). The group both categorizes a node for the client and fixes\nits place in the DETERMINISTIC node order: nodes are emitted by ascending\n`(group.rank(), sequence)`, so identical config yields identical order.",
      "oneOf": [
        {
          "const": "default",
          "description": "Cross cutting nodes shared by every method (the identifier field, hidden fields).",
          "type": "string"
        },
        {
          "const": "password",
          "description": "The password method.",
          "type": "string"
        },
        {
          "const": "passkey",
          "description": "The passkey (WebAuthn) method.",
          "type": "string"
        },
        {
          "const": "totp",
          "description": "The TOTP second factor method.",
          "type": "string"
        },
        {
          "const": "email_otp",
          "description": "The email one time code method.",
          "type": "string"
        },
        {
          "const": "sms_otp",
          "description": "The SMS one time code method.",
          "type": "string"
        },
        {
          "const": "recovery_code",
          "description": "The recovery code method.",
          "type": "string"
        },
        {
          "const": "oidc",
          "description": "A federated (OIDC upstream) method.",
          "type": "string"
        },
        {
          "const": "profile",
          "description": "Profile fields collected during registration.",
          "type": "string"
        },
        {
          "const": "submit",
          "description": "The terminal submit action, rendered after every collected field.",
          "type": "string"
        }
      ]
    },
    "Transport": {
      "description": "The transport a flow was created on (issue #84). Set at creation and immutable, so a\nflow created for a native JSON client is never continued as a browser flow.",
      "oneOf": [
        {
          "const": "browser",
          "description": "The browser transport: form posts, the same origin CSRF gate, and cookie plus 303\nredirect continuation.",
          "type": "string"
        },
        {
          "const": "api",
          "description": "The API transport: `application/json` submission with a per flow submit token, and\na 200 JSON envelope continuation.",
          "type": "string"
        }
      ]
    },
    "Ui": {
      "description": "The UI container (issue #84): the submission target, the method, the ordered nodes, and\nthe flow level messages.",
      "properties": {
        "action": {
          "description": "Where the client submits the flow.",
          "type": "string"
        },
        "messages": {
          "description": "The flow level messages (errors and info not attached to a single node).",
          "items": {
            "$ref": "#/$defs/Message"
          },
          "type": "array"
        },
        "method": {
          "description": "The submission method (informational for the API transport).",
          "type": "string"
        },
        "nodes": {
          "description": "The nodes, in the DETERMINISTIC contract order (see [`Ui::sorted`]).",
          "items": {
            "$ref": "#/$defs/Node"
          },
          "type": "array"
        }
      },
      "required": [
        "action",
        "method",
        "nodes",
        "messages"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "The whole flow object (issue #84): the ONE thing both transports render. The\n`submit_token` is NOT here; it rides the API transport envelope only, so the object is\nbyte identical across transports.",
  "properties": {
    "contract_version": {
      "description": "The flow contract version (FORK B). Also mirrored in the response header.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "expires_at": {
      "description": "The flow expiry as a unix timestamp in seconds (from the app clock seam).",
      "format": "int64",
      "type": "integer"
    },
    "id": {
      "description": "The scope embedded `flw_` id.",
      "type": "string"
    },
    "journey": {
      "$ref": "#/$defs/Journey",
      "description": "The journey this flow drives."
    },
    "request_url": {
      "description": "The pending `/authorize` resume target, or [`None`].",
      "type": [
        "string",
        "null"
      ]
    },
    "state": {
      "$ref": "#/$defs/FlowStateTag",
      "description": "The current state machine position."
    },
    "transport": {
      "$ref": "#/$defs/Transport",
      "description": "The transport this flow was created on (immutable)."
    },
    "ui": {
      "$ref": "#/$defs/Ui",
      "description": "The UI: ordered nodes plus flow level messages."
    }
  },
  "required": [
    "contract_version",
    "id",
    "journey",
    "state",
    "transport",
    "expires_at",
    "ui"
  ],
  "title": "Flow",
  "type": "object"
}
