{
  "$comment": "Generated by scripts/journey-schema.sh; do not edit by hand.",
  "$defs": {
    "CmpOp": {
      "description": "A comparison operator (issue #92): the closed set of ordering and equality operators the\npredicate grammar permits. The wire form is the `snake_case` tag.",
      "oneOf": [
        {
          "const": "eq",
          "description": "Equal.",
          "type": "string"
        },
        {
          "const": "ne",
          "description": "Not equal.",
          "type": "string"
        },
        {
          "const": "lt",
          "description": "Less than.",
          "type": "string"
        },
        {
          "const": "le",
          "description": "Less than or equal.",
          "type": "string"
        },
        {
          "const": "gt",
          "description": "Greater than.",
          "type": "string"
        },
        {
          "const": "ge",
          "description": "Greater than or equal.",
          "type": "string"
        }
      ]
    },
    "DecisionSpec": {
      "description": "A decision a branch step evaluates (issue #92). Modeled as an enum so a later milestone can\nadd an alternative (a sandboxed evaluator) without a breaking shape change; PR 1 reserves\nthe shape and defines only the predicate form. Tagged by `type` on the wire.",
      "oneOf": [
        {
          "description": "A decision expressed as a predicate over the typed evaluation context.",
          "properties": {
            "predicate": {
              "$ref": "#/$defs/Predicate",
              "description": "The predicate this decision evaluates."
            },
            "type": {
              "const": "predicate",
              "type": "string"
            }
          },
          "required": [
            "type",
            "predicate"
          ],
          "type": "object"
        }
      ]
    },
    "FieldRef": {
      "additionalProperties": false,
      "description": "A typed reference to a field of the evaluation context (issue #92): the source it is read\nfrom and an RFC 6901 JSON Pointer into that source. The evaluation context is assembled by\nthe engine before the predicate is evaluated (PR 2); PR 1 defines the reference as data. An\nunknown property is a hard parse error (`deny_unknown_fields`).",
      "properties": {
        "pointer": {
          "description": "The RFC 6901 JSON Pointer into the source.",
          "type": "string"
        },
        "source": {
          "$ref": "#/$defs/FieldSource",
          "description": "The context source this field is read from."
        }
      },
      "required": [
        "source",
        "pointer"
      ],
      "type": "object"
    },
    "FieldSource": {
      "description": "The typed sources a [`FieldRef`] can read from (issue #92): a closed set. Flow-local state,\nstep outcome signals, the subject's sealed traits, the subject's groups and scopes, and the\nrisk decision. The wire form is the `snake_case` tag.",
      "oneOf": [
        {
          "const": "flow",
          "description": "Flow-local state (the step id, the proven method tokens).",
          "type": "string"
        },
        {
          "const": "signals",
          "description": "Step outcome signals (for example `mfa_required`, `primary_verified`).",
          "type": "string"
        },
        {
          "const": "subject_traits",
          "description": "The subject's sealed identity traits.",
          "type": "string"
        },
        {
          "const": "subject_groups",
          "description": "The subject's group memberships.",
          "type": "string"
        },
        {
          "const": "subject_scopes",
          "description": "The subject's granted scopes.",
          "type": "string"
        },
        {
          "const": "risk",
          "description": "The risk decision.",
          "type": "string"
        }
      ]
    },
    "Literal": {
      "anyOf": [
        {
          "description": "A JSON null.",
          "type": "null"
        },
        {
          "description": "A boolean.",
          "type": "boolean"
        },
        {
          "description": "A number.",
          "type": "number"
        },
        {
          "description": "A string.",
          "type": "string"
        }
      ],
      "description": "A literal value in the predicate grammar (issue #92): the CLOSED set of primitive JSON\nvalues a comparison or membership test permits. An object or an array is NOT a literal. The\nwire form is the bare JSON value (untagged)."
    },
    "MemberSet": {
      "description": "The set a [`Predicate::Member`] tests the subject against (issue #92): a named group or a\nnamed scope. Tagged by `kind` on the wire.",
      "oneOf": [
        {
          "description": "A named subject group.",
          "properties": {
            "kind": {
              "const": "group",
              "type": "string"
            },
            "name": {
              "description": "The group name.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ],
          "type": "object"
        },
        {
          "description": "A named subject scope.",
          "properties": {
            "kind": {
              "const": "scope",
              "type": "string"
            },
            "name": {
              "description": "The scope name.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ],
          "type": "object"
        }
      ]
    },
    "Predicate": {
      "description": "The predicate AST (issue #92): a minimal, closed grammar over comparisons, membership, and\nboolean combinators, expressed as DATA. PR 1 defines it and validates its STRUCTURE (the\nserde shape); it does NOT evaluate it and does NOT unify its types. The evaluator and the\nload-time type check are PR 2. Tagged by `type` on the wire.",
      "oneOf": [
        {
          "description": "A comparison of a field against a literal under a comparison operator.",
          "properties": {
            "field": {
              "$ref": "#/$defs/FieldRef",
              "description": "The field being compared."
            },
            "op": {
              "$ref": "#/$defs/CmpOp",
              "description": "The comparison operator."
            },
            "type": {
              "const": "cmp",
              "type": "string"
            },
            "value": {
              "$ref": "#/$defs/Literal",
              "description": "The literal the field is compared against."
            }
          },
          "required": [
            "type",
            "field",
            "op",
            "value"
          ],
          "type": "object"
        },
        {
          "description": "Whether a field's value is one of a closed set of literals.",
          "properties": {
            "field": {
              "$ref": "#/$defs/FieldRef",
              "description": "The field being tested."
            },
            "type": {
              "const": "in",
              "type": "string"
            },
            "values": {
              "description": "The permitted literals.",
              "items": {
                "$ref": "#/$defs/Literal"
              },
              "type": "array"
            }
          },
          "required": [
            "type",
            "field",
            "values"
          ],
          "type": "object"
        },
        {
          "description": "Whether the subject is a member of a named group or scope set.",
          "properties": {
            "field": {
              "$ref": "#/$defs/FieldRef",
              "description": "The field the membership is read from."
            },
            "set": {
              "$ref": "#/$defs/MemberSet",
              "description": "The set the subject is tested against."
            },
            "type": {
              "const": "member",
              "type": "string"
            }
          },
          "required": [
            "type",
            "field",
            "set"
          ],
          "type": "object"
        },
        {
          "description": "A conjunction: every operand must hold.",
          "properties": {
            "operands": {
              "description": "The operands, all of which must hold.",
              "items": {
                "$ref": "#/$defs/Predicate"
              },
              "type": "array"
            },
            "type": {
              "const": "and",
              "type": "string"
            }
          },
          "required": [
            "type",
            "operands"
          ],
          "type": "object"
        },
        {
          "description": "A disjunction: at least one operand must hold.",
          "properties": {
            "operands": {
              "description": "The operands, at least one of which must hold.",
              "items": {
                "$ref": "#/$defs/Predicate"
              },
              "type": "array"
            },
            "type": {
              "const": "or",
              "type": "string"
            }
          },
          "required": [
            "type",
            "operands"
          ],
          "type": "object"
        },
        {
          "description": "A negation of a single operand.",
          "properties": {
            "operand": {
              "$ref": "#/$defs/Predicate",
              "description": "The operand being negated."
            },
            "type": {
              "const": "not",
              "type": "string"
            }
          },
          "required": [
            "type",
            "operand"
          ],
          "type": "object"
        },
        {
          "description": "The constant true predicate.",
          "properties": {
            "type": {
              "const": "always",
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The constant false predicate.",
          "properties": {
            "type": {
              "const": "never",
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "Step": {
      "additionalProperties": false,
      "description": "One step of a journey (issue #92): a document-local id, the built-in kind that executes\nit, and the kind-specific attachments (the node group it renders under, the subflow it\ncalls, or the decision it evaluates). Comments are data fields. An unknown property is a\nhard parse error (`deny_unknown_fields`).",
      "properties": {
        "comment": {
          "description": "An optional human-readable comment about the step (data, round-trip-safe).",
          "type": [
            "string",
            "null"
          ]
        },
        "decision": {
          "anyOf": [
            {
              "$ref": "#/$defs/DecisionSpec"
            },
            {
              "type": "null"
            }
          ],
          "description": "The decision this step evaluates, for a [`StepKind::Decision`] step."
        },
        "id": {
          "description": "The document-local step id (unique across the journey's steps).",
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/StepKind",
          "description": "The built-in step kind that executes this step."
        },
        "node_group": {
          "description": "The node group this step renders under, when the kind renders a UI (validated against\nthe existing node-group vocabulary; see [`crate::NODE_GROUPS`]).",
          "type": [
            "string",
            "null"
          ]
        },
        "subflow": {
          "description": "The subflow this step calls, for a [`StepKind::SubflowCall`] step (a reference into the\njourney's `subflows` list).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "kind"
      ],
      "type": "object"
    },
    "StepKind": {
      "description": "A built-in journey step kind (the closed orchestration vocabulary).",
      "enum": [
        "identifier_password",
        "mfa_challenge",
        "mfa_enroll",
        "progressive_profiling",
        "decision",
        "subflow_call",
        "terminal"
      ],
      "type": "string"
    },
    "SubflowRef": {
      "additionalProperties": false,
      "description": "A subflow reference a journey can call (issue #92): a document-local id and its source. PR\n1 parses and validates the SHAPE only; full subflow composition is PR 3. An unknown\nproperty is a hard parse error (`deny_unknown_fields`).",
      "properties": {
        "id": {
          "description": "The document-local subflow id a [`StepKind::SubflowCall`] step references.",
          "type": "string"
        },
        "source": {
          "$ref": "#/$defs/SubflowSource",
          "description": "Where the subflow's definition comes from."
        }
      },
      "required": [
        "id",
        "source"
      ],
      "type": "object"
    },
    "SubflowSource": {
      "description": "The source of a subflow reference (issue #92). Kept minimal for PR 1: a built-in subflow\nthe engine provides, or an inline reference by id (full inline composition is PR 3). Tagged\nby `kind` on the wire.",
      "oneOf": [
        {
          "description": "A built-in subflow the engine provides, named (for example `mfa_step_up`).",
          "properties": {
            "kind": {
              "const": "builtin",
              "type": "string"
            },
            "name": {
              "description": "The built-in subflow name.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "name"
          ],
          "type": "object"
        },
        {
          "description": "An inline subflow referenced by id; the inline body lands in PR 3.",
          "properties": {
            "kind": {
              "const": "inline",
              "type": "string"
            },
            "subflow_id": {
              "description": "The referenced inline subflow id.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "subflow_id"
          ],
          "type": "object"
        }
      ]
    },
    "Transition": {
      "additionalProperties": false,
      "description": "A transition between two steps (issue #92): the source step, the target step, and an\noptional guard predicate that must hold for the transition to be taken. An UNGUARDED\ntransition (a `guard` of [`None`]) always applies; two or more unguarded transitions from\nthe same step are ambiguous and the validator rejects them\n([`crate::JourneyError::NonDeterministicTransition`]). An unknown property (a misspelled\n`guard`, for example) is a hard parse error (`deny_unknown_fields`), so it can never\nsilently drop the guard and change routing.",
      "properties": {
        "comment": {
          "description": "An optional human-readable comment about the transition (data, round-trip-safe).",
          "type": [
            "string",
            "null"
          ]
        },
        "from": {
          "description": "The source step id.",
          "type": "string"
        },
        "guard": {
          "anyOf": [
            {
              "$ref": "#/$defs/Predicate"
            },
            {
              "type": "null"
            }
          ],
          "description": "The guard predicate, or [`None`] for an unguarded (always-applicable) transition."
        },
        "to": {
          "description": "The target step id.",
          "type": "string"
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A whole journey artifact (issue #92): the declarative topology and routing a flow runs.\nIt carries the format tag, the authored engine version, the entry step, the step list,\nthe transitions between steps, and any subflow references. Comments are FIRST-CLASS data\nfields, preserved through a serde round-trip, never lexical trivia.\n\nAn unknown or misspelled property is a HARD parse error (`deny_unknown_fields`), so a typo\nlike `gaurd` cannot silently drop a guard and change routing; the published schema mirrors\nthis with `additionalProperties: false`.",
  "properties": {
    "comment": {
      "description": "An optional human-readable comment about the journey (data, round-trip-safe).",
      "type": [
        "string",
        "null"
      ]
    },
    "engine_version": {
      "description": "The orchestration ABI version this artifact was authored for; the validator refuses a\nvalue greater than [`JOURNEY_ENGINE_VERSION`].",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "entry": {
      "description": "The entry step: the step id the journey starts at.",
      "type": "string"
    },
    "id": {
      "description": "The document-local journey identifier.",
      "type": "string"
    },
    "schema_version": {
      "description": "The artifact format tag; the canonical value is [`JOURNEY_SCHEMA_VERSION`].",
      "type": "string"
    },
    "steps": {
      "description": "The steps this journey can occupy, in document order.",
      "items": {
        "$ref": "#/$defs/Step"
      },
      "type": "array"
    },
    "subflows": {
      "description": "The subflow references this journey can call, or [`None`] when it calls none.",
      "items": {
        "$ref": "#/$defs/SubflowRef"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "transitions": {
      "description": "The transitions between steps, in document order.",
      "items": {
        "$ref": "#/$defs/Transition"
      },
      "type": "array"
    }
  },
  "required": [
    "schema_version",
    "id",
    "engine_version",
    "entry",
    "steps",
    "transitions"
  ],
  "title": "Journey",
  "type": "object"
}
