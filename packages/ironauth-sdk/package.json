{
  "name": "@ironauth/sdk",
  "version": "0.0.0",
  "private": true,
  "description": "The IronAuth JavaScript SDK: a uniform authorization check() that resolves via token claims, the AuthZEN PDP, or a customer PDP by configuration (issue #100).",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "tsc && node --test \"dist/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
