{
  "name": "@ironauth/docs-mcp",
  "version": "0.0.0",
  "private": true,
  "description": "The IronAuth docs MCP server (issue #123): search and retrieval over the published documentation, so a coding agent pulls current guidance instead of recalling it.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "tsc && node --test \"dist/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
