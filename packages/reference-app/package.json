{
  "name": "@ironauth/reference-app",
  "version": "0.0.0",
  "private": true,
  "description": "Standalone forkable reference app that renders the IronAuth headless flow contract over the public flow API only.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "scripts": {
    "codegen": "python3 ../../scripts/gen-reference-app-bindings.py",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
